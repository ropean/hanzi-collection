# 汉字集 - 项目规划方案

> 一个简洁大气的汉字学习与诗词欣赏平台

---

## 📋 项目概述

### 项目定位
面向五笔学习者和古典文学爱好者的工具型网站，核心功能为**86五笔字根查询**和**唐诗宋词欣赏**。

### 设计理念
- **简洁**：去除冗余，聚焦核心功能
- **大气**：留白充足，呼吸感强
- **耐看**：中性配色，长时间使用不疲劳

---

## 🎯 核心功能模块

### 一、汉字查询模块（主功能）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 五笔编码查询 | 输入汉字显示86五笔编码 | P0 |
| 字根拆解展示 | 显示汉字的字根拆分图解 | P0 |
| 拼音查询 | 支持通过拼音反查汉字 | P1 |
| 笔顺动画 | 展示汉字书写笔顺 | P1 |
| 手写识别 | Canvas 手写输入识别汉字 | P2 |

### 二、诗词欣赏模块

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 唐诗浏览 | 按作者/朝代/主题分类浏览 | P0 |
| 宋词浏览 | 按词人/词牌名分类浏览 | P0 |
| 全文搜索 | 按标题/作者/内容搜索 | P1 |
| 诗词详情 | 展示原文、注释、赏析 | P1 |
| 随机推荐 | 每日一诗/词推荐 | P2 |

---

## 🛠 技术架构

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端应用 (SPA)                          │
│              Vue 3 + Vite + TailwindCSS                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │  汉字查询    │  │  诗词欣赏   │  │     通用组件        │ │
│  │             │  │             │  │                     │ │
│  │ • 搜索框    │  │ • 诗词列表  │  │ • 搜索栏            │ │
│  │ • 字根展示  │  │ • 详情页    │  │ • 导航栏            │ │
│  │ • 笔顺动画  │  │ • 作者页    │  │ • 分页器            │ │
│  │ • 手写板    │  │ • 随机推荐  │  │ • 加载状态          │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                       核心依赖库                             │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐│
│  │ hanzi-writer │ │  pinyin-pro  │ │   HanziLookupJS     ││
│  │ 笔顺动画     │ │  拼音处理    │ │   手写识别           ││
│  └──────────────┘ └──────────────┘ └──────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│                      数据层 (静态JSON)                       │
│   86五笔编码  │  字根图片  │  唐诗  │  宋词  │  作者信息    │
└─────────────────────────────────────────────────────────────┘
```

### 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Vue 3 + Composition API | 轻量、响应式、生态成熟 |
| 构建 | Vite | 极速开发体验 |
| 样式 | TailwindCSS | 原子化CSS，快速实现设计 |
| 路由 | Vue Router 4 | SPA 路由管理 |
| 状态 | Pinia | 轻量级状态管理 |
| 搜索 | Fuse.js | 前端模糊搜索 |
| 部署 | Cloudflare Pages | 免费、全球CDN、自动部署 |

---

## 📦 核心依赖库详解

### 1. hanzi-writer（笔顺动画）

**项目地址**：https://github.com/chanind/hanzi-writer

**功能**：
- 汉字笔顺动画播放
- 书写练习模式
- 笔画高亮展示
- 支持简繁体 9500+ 汉字

**安装**：
```bash
npm install hanzi-writer
```

**基础用法**：
```javascript
import HanziWriter from 'hanzi-writer'

// 创建实例
const writer = HanziWriter.create('target-div', '汉', {
  width: 150,
  height: 150,
  padding: 5,
  strokeAnimationSpeed: 1,
  delayBetweenStrokes: 200,
  strokeColor: '#333',
  radicalColor: '#168F16'  // 字根高亮色
})

// 播放动画
writer.animateCharacter()

// 开启练习模式
writer.quiz({
  onComplete: (summaryData) => {
    console.log('练习完成', summaryData)
  }
})
```

### 2. pinyin-pro（拼音处理）

**项目地址**：https://github.com/zh-lx/pinyin-pro

**功能**：
- 汉字转拼音（带声调）
- 多音字智能识别（99.8%准确率）
- 拼音匹配搜索
- 首字母/声母/韵母提取

**安装**：
```bash
npm install pinyin-pro
```

**基础用法**：
```javascript
import { pinyin, match } from 'pinyin-pro'

// 获取拼音
pinyin('汉语拼音')           // 'hàn yǔ pīn yīn'
pinyin('汉语拼音', { toneType: 'none' })  // 'han yu pin yin'
pinyin('汉语拼音', { type: 'array' })     // ['hàn', 'yǔ', 'pīn', 'yīn']

// 多音字自动识别
pinyin('睡着了')             // 'shuì zháo le'

// 拼音搜索匹配
match('中文拼音', 'zwpy')    // [0, 1, 2, 3] 匹配到的字符索引
match('中文拼音', 'zhongwen') // [0, 1]
```

### 3. HanziLookupJS（手写识别）

**项目地址**：https://github.com/gugray/HanziLookupJS

**功能**：
- 纯前端手写汉字识别
- 基于 Make Me a Hanzi 数据（9507字）
- 无需网络请求

**安装**：
```bash
# 下载 hanzilookup.min.js 和 mmah.json 数据文件
```

**基础用法**：
```javascript
// 初始化
HanziLookup.init('mmah', '/data/mmah.json', (success) => {
  if (success) console.log('手写识别初始化完成')
})

// 识别（strokes 为笔画数据）
const matcher = new HanziLookup.Matcher('mmah')
const matches = matcher.match(strokes, 8)  // 返回最多8个候选字
```

---

## 📂 数据来源与结构

### 1. 86五笔编码数据

**来源**：https://github.com/wongsingfo/86wubi

**数据结构**：
```json
{
  "汉": {
    "wubi": "ICY",
    "pinyin": "hàn",
    "radicals": ["氵", "又"],
    "strokes": 5,
    "image": "/images/wubi/汉.png"
  }
}
```

**字根拆解图片**：
- 来源：wb86.com / 52wubi.com
- 覆盖：《通用规范汉字表》一级字表 + 二级字表（3355字）

### 2. 唐诗宋词数据

**来源**：https://github.com/chinese-poetry/chinese-poetry

**数据规模**：
| 类型 | 数量 | 说明 |
|------|------|------|
| 唐诗 | 5.5万首 | 近1.4万诗人 |
| 宋诗 | 26万首 | - |
| 宋词 | 2.1万首 | 1564位词人 |

**数据结构**（唐诗）：
```json
{
  "title": "静夜思",
  "author": "李白",
  "paragraphs": [
    "床前明月光，",
    "疑是地上霜。",
    "举头望明月，",
    "低头思故乡。"
  ],
  "tags": ["思乡", "月亮"]
}
```

**数据结构**（宋词）：
```json
{
  "rhythmic": "水调歌头",
  "author": "苏轼",
  "paragraphs": [
    "明月几时有？把酒问青天。",
    "..."
  ]
}
```

### 3. 推荐数据精选方案

考虑到完整数据量巨大（数十万首），建议分层加载：

| 层级 | 内容 | 数据量 | 加载方式 |
|------|------|--------|----------|
| 核心 | 唐诗三百首 + 宋词三百首 | ~600首 | 首屏加载 |
| 扩展 | 名家代表作 | ~5000首 | 按需加载 |
| 完整 | 全部诗词 | 30万+ | 搜索时加载 |

---

## 📁 项目目录结构

```
hanzi-collection/
├── public/
│   ├── data/
│   │   ├── wubi/
│   │   │   ├── wubi86.json          # 86五笔编码表
│   │   │   └── images/              # 字根拆解图片
│   │   ├── poetry/
│   │   │   ├── tang300.json         # 唐诗三百首
│   │   │   ├── song300.json         # 宋词三百首
│   │   │   ├── authors.json         # 作者信息
│   │   │   └── full/                # 完整数据（按需加载）
│   │   └── handwriting/
│   │       └── mmah.json            # 手写识别数据
│   └── fonts/
│       └── LXGWWenKai.woff2         # 霞鹜文楷字体
│
├── src/
│   ├── assets/
│   │   └── styles/
│   │       ├── main.css             # 全局样式
│   │       └── typography.css       # 字体排版
│   │
│   ├── components/
│   │   ├── common/
│   │   │   ├── SearchBar.vue        # 统一搜索组件
│   │   │   ├── NavBar.vue           # 顶部导航
│   │   │   └── Pagination.vue       # 分页组件
│   │   │
│   │   ├── hanzi/
│   │   │   ├── HanziCard.vue        # 汉字信息卡片
│   │   │   ├── WubiDisplay.vue      # 五笔编码展示
│   │   │   ├── StrokePlayer.vue     # 笔顺动画播放器
│   │   │   ├── RadicalImage.vue     # 字根拆解图
│   │   │   └── HandwritingPad.vue   # 手写输入板
│   │   │
│   │   └── poetry/
│   │       ├── PoemCard.vue         # 诗词卡片
│   │       ├── PoemDetail.vue       # 诗词详情
│   │       ├── AuthorCard.vue       # 作者卡片
│   │       └── DailyPoem.vue        # 每日推荐
│   │
│   ├── views/
│   │   ├── Home.vue                 # 首页
│   │   ├── HanziSearch.vue          # 汉字查询页
│   │   ├── PoetryList.vue           # 诗词列表页
│   │   ├── PoetryDetail.vue         # 诗词详情页
│   │   ├── AuthorPage.vue           # 作者页面
│   │   └── About.vue                # 关于页面
│   │
│   ├── stores/
│   │   ├── hanzi.js                 # 汉字数据状态
│   │   ├── poetry.js                # 诗词数据状态
│   │   └── search.js                # 搜索历史
│   │
│   ├── composables/
│   │   ├── useHanziWriter.js        # hanzi-writer 封装
│   │   ├── usePinyin.js             # pinyin-pro 封装
│   │   ├── useHandwriting.js        # 手写识别封装
│   │   └── useSearch.js             # 搜索逻辑封装
│   │
│   ├── utils/
│   │   ├── dataLoader.js            # 数据加载工具
│   │   └── storage.js               # 本地存储工具
│   │
│   ├── router/
│   │   └── index.js                 # 路由配置
│   │
│   ├── App.vue
│   └── main.js
│
├── scripts/
│   ├── build-wubi-data.js           # 五笔数据预处理
│   └── build-poetry-data.js         # 诗词数据预处理
│
├── index.html
├── vite.config.js
├── tailwind.config.js
├── package.json
└── README.md
```

---

## 🎨 UI/UX 设计规范

### 配色方案

采用低饱和度中性色系，营造沉稳书卷气：

```css
:root {
  /* 主色 - 墨色系 */
  --color-ink: #2c3e50;
  --color-ink-light: #5d6d7e;
  
  /* 背景色 */
  --color-bg: #fafafa;
  --color-bg-card: #ffffff;
  --color-bg-hover: #f5f5f5;
  
  /* 强调色 - 朱砂红（少量点缀） */
  --color-accent: #c0392b;
  
  /* 文字色 */
  --color-text: #333333;
  --color-text-secondary: #666666;
  --color-text-muted: #999999;
  
  /* 边框色 */
  --color-border: #e0e0e0;
}
```

### 字体方案

```css
/* 正文：系统字体栈 */
--font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", 
             "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei",
             sans-serif;

/* 诗词展示：霞鹜文楷（可选加载） */
--font-poetry: "LXGW WenKai", "楷体", "KaiTi", serif;

/* 五笔编码：等宽字体 */
--font-mono: "JetBrains Mono", "Fira Code", monospace;
```

### 布局原则

1. **大量留白**：元素间距充足，视觉呼吸感强
2. **内容居中**：最大宽度 1200px，两侧留边
3. **卡片化设计**：信息以卡片承载，边界清晰
4. **响应式断点**：
   - Mobile: < 640px
   - Tablet: 640px - 1024px
   - Desktop: > 1024px

### 核心页面线框图

#### 首页
```
┌─────────────────────────────────────────────────────────┐
│  LOGO          [汉字]  [诗词]  [关于]                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    ┌─────────────────┐                  │
│                    │   搜索框        │                  │
│                    │   五笔/拼音/汉字│                  │
│                    └─────────────────┘                  │
│                                                         │
│      ┌──────────────────┐  ┌──────────────────┐        │
│      │                  │  │                  │        │
│      │   五笔查字       │  │   诗词欣赏       │        │
│      │   快速入口       │  │   快速入口       │        │
│      │                  │  │                  │        │
│      └──────────────────┘  └──────────────────┘        │
│                                                         │
│                    ┌──────────────────┐                 │
│                    │   今日推荐       │                 │
│                    │   [一首诗词]     │                 │
│                    └──────────────────┘                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 汉字详情页
```
┌─────────────────────────────────────────────────────────┐
│  ← 返回       [搜索框]                                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│     ┌──────────┐    ┌────────────────────────────┐     │
│     │          │    │  汉                         │     │
│     │    汉    │    │                            │     │
│     │  (笔顺)  │    │  拼音：hàn                 │     │
│     │          │    │  五笔：ICY                 │     │
│     │  [播放]  │    │  笔画：5                   │     │
│     └──────────┘    │  部首：氵                  │     │
│                     └────────────────────────────┘     │
│                                                         │
│     ┌─────────────────────────────────────────────┐    │
│     │  字根拆解                                    │    │
│     │  ┌────────────────────────────────────┐    │    │
│     │  │         [字根拆解图片]              │    │    │
│     │  └────────────────────────────────────┘    │    │
│     └─────────────────────────────────────────────┘    │
│                                                         │
│     ┌─────────────────────────────────────────────┐    │
│     │  手写输入                    [清除] [识别]  │    │
│     │  ┌────────────────────────────────────┐    │    │
│     │  │                                    │    │    │
│     │  │         [手写区域]                 │    │    │
│     │  │                                    │    │    │
│     │  └────────────────────────────────────┘    │    │
│     └─────────────────────────────────────────────┘    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### 诗词详情页
```
┌─────────────────────────────────────────────────────────┐
│  ← 返回       [搜索框]                                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    静 夜 思                             │
│                    ─────────                            │
│                    [唐] 李白                            │
│                                                         │
│              床前明月光，疑是地上霜。                    │
│              举头望明月，低头思故乡。                    │
│                                                         │
│     ─────────────────────────────────────────────      │
│                                                         │
│     注释                                                │
│     床：此诗中的"床"字，是争论和异议的焦点...          │
│                                                         │
│     ─────────────────────────────────────────────      │
│                                                         │
│     赏析                                                │
│     这是写远客思乡之情的诗，诗以明白如话的语言...       │
│                                                         │
│     ─────────────────────────────────────────────      │
│                                                         │
│     作者简介                                            │
│     李白（701—762），字太白，号青莲居士...             │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 开发路线图

### Phase 1：MVP（2-3周）

**目标**：核心功能可用

| 任务 | 优先级 | 预估工时 |
|------|--------|----------|
| 项目初始化（Vue3 + Vite + Tailwind） | P0 | 0.5天 |
| 基础布局与导航组件 | P0 | 1天 |
| 86五笔数据整合与处理 | P0 | 1天 |
| 汉字搜索页（五笔/拼音/汉字） | P0 | 2天 |
| 汉字详情页（编码 + 字根图） | P0 | 1.5天 |
| 集成 hanzi-writer 笔顺动画 | P0 | 1天 |
| 唐诗三百首数据整合 | P0 | 0.5天 |
| 诗词列表与详情页 | P0 | 2天 |
| 响应式适配 | P0 | 1天 |
| 部署上线（Cloudflare Pages） | P0 | 0.5天 |

**交付物**：
- [x] 可访问的在线网站
- [x] 五笔查字功能完整可用
- [x] 唐诗三百首浏览功能

### Phase 2：功能完善（2-3周）

| 任务 | 优先级 | 预估工时 |
|------|--------|----------|
| 手写识别功能（HanziLookupJS） | P1 | 2天 |
| 宋词三百首数据整合 | P1 | 0.5天 |
| 作者页面 | P1 | 1天 |
| 搜索历史与收藏功能 | P1 | 1.5天 |
| 高级搜索（按朝代/作者/词牌） | P1 | 1.5天 |
| 每日推荐功能 | P2 | 1天 |
| UI 细节优化 | P1 | 2天 |
| 性能优化（懒加载、缓存） | P1 | 1天 |

**交付物**：
- [x] 手写输入识别
- [x] 完整诗词浏览体验
- [x] 个性化功能（收藏、历史）

### Phase 3：增强体验（2-3周）

| 任务 | 优先级 | 预估工时 |
|------|--------|----------|
| 完整诗词数据接入（按需加载） | P2 | 2天 |
| 书写练习模式 | P2 | 2天 |
| PWA 离线支持 | P2 | 1.5天 |
| 深色模式 | P2 | 1天 |
| 字体自定义设置 | P3 | 1天 |
| 分享功能 | P3 | 1天 |

---

## 🔧 关键实现细节

### 1. 统一搜索逻辑

```javascript
// composables/useSearch.js
import { pinyin, match } from 'pinyin-pro'
import Fuse from 'fuse.js'

export function useSearch(hanziData, poetryData) {
  // 判断输入类型
  const detectInputType = (input) => {
    if (/^[a-z]+$/i.test(input)) {
      // 纯字母：可能是五笔编码或拼音
      if (input.length <= 4) return 'wubi_or_pinyin'
      return 'pinyin'
    }
    if (/[\u4e00-\u9fa5]/.test(input)) return 'hanzi'
    return 'mixed'
  }

  // 搜索汉字
  const searchHanzi = (query) => {
    const type = detectInputType(query)
    
    if (type === 'hanzi') {
      // 直接查找汉字
      return hanziData.value.filter(h => h.char === query)
    }
    
    if (type === 'wubi_or_pinyin') {
      // 同时搜索五笔和拼音
      const wubiResults = hanziData.value.filter(h => 
        h.wubi.toLowerCase().startsWith(query.toLowerCase())
      )
      const pinyinResults = hanziData.value.filter(h => 
        match(h.char, query).length > 0
      )
      return [...new Set([...wubiResults, ...pinyinResults])]
    }
    
    return []
  }

  // 搜索诗词
  const searchPoetry = (query) => {
    const fuse = new Fuse(poetryData.value, {
      keys: ['title', 'author', 'paragraphs'],
      threshold: 0.3
    })
    return fuse.search(query).map(r => r.item)
  }

  return { searchHanzi, searchPoetry, detectInputType }
}
```

### 2. 笔顺动画组件

```vue
<!-- components/hanzi/StrokePlayer.vue -->
<template>
  <div class="stroke-player">
    <div ref="writerRef" class="writer-container"></div>
    <div class="controls">
      <button @click="animate" :disabled="isAnimating">
        播放笔顺
      </button>
      <button @click="quiz">
        练习模式
      </button>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted, watch } from 'vue'
import HanziWriter from 'hanzi-writer'

const props = defineProps({
  char: { type: String, required: true },
  size: { type: Number, default: 150 }
})

const writerRef = ref(null)
const writer = ref(null)
const isAnimating = ref(false)

const initWriter = () => {
  if (writer.value) writer.value = null
  
  writer.value = HanziWriter.create(writerRef.value, props.char, {
    width: props.size,
    height: props.size,
    padding: 5,
    strokeAnimationSpeed: 1,
    delayBetweenStrokes: 300,
    strokeColor: '#333',
    radicalColor: '#c0392b',
    showOutline: true
  })
}

const animate = () => {
  isAnimating.value = true
  writer.value.animateCharacter({
    onComplete: () => { isAnimating.value = false }
  })
}

const quiz = () => {
  writer.value.quiz({
    onMistake: (data) => { console.log('错误', data) },
    onComplete: (data) => { console.log('完成', data) }
  })
}

onMounted(initWriter)
watch(() => props.char, initWriter)
</script>
```

### 3. 手写识别组件

```vue
<!-- components/hanzi/HandwritingPad.vue -->
<template>
  <div class="handwriting-pad">
    <canvas 
      ref="canvasRef"
      @mousedown="startStroke"
      @mousemove="continueStroke"
      @mouseup="endStroke"
      @touchstart.prevent="startStroke"
      @touchmove.prevent="continueStroke"
      @touchend="endStroke"
    ></canvas>
    <div class="candidates">
      <button 
        v-for="char in candidates" 
        :key="char"
        @click="$emit('select', char)"
      >
        {{ char }}
      </button>
    </div>
    <div class="actions">
      <button @click="clear">清除</button>
      <button @click="recognize">识别</button>
    </div>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const emit = defineEmits(['select'])

const canvasRef = ref(null)
const ctx = ref(null)
const strokes = ref([])
const currentStroke = ref([])
const candidates = ref([])
const isDrawing = ref(false)

onMounted(async () => {
  // 初始化 Canvas
  const canvas = canvasRef.value
  ctx.value = canvas.getContext('2d')
  ctx.value.lineWidth = 5
  ctx.value.lineCap = 'round'
  ctx.value.strokeStyle = '#333'
  
  // 初始化手写识别库
  await initHanziLookup()
})

const initHanziLookup = () => {
  return new Promise((resolve) => {
    HanziLookup.init('mmah', '/data/handwriting/mmah.json', resolve)
  })
}

const getPoint = (e) => {
  const rect = canvasRef.value.getBoundingClientRect()
  const clientX = e.touches ? e.touches[0].clientX : e.clientX
  const clientY = e.touches ? e.touches[0].clientY : e.clientY
  return {
    x: clientX - rect.left,
    y: clientY - rect.top
  }
}

const startStroke = (e) => {
  isDrawing.value = true
  const point = getPoint(e)
  currentStroke.value = [point]
  ctx.value.beginPath()
  ctx.value.moveTo(point.x, point.y)
}

const continueStroke = (e) => {
  if (!isDrawing.value) return
  const point = getPoint(e)
  currentStroke.value.push(point)
  ctx.value.lineTo(point.x, point.y)
  ctx.value.stroke()
}

const endStroke = () => {
  if (!isDrawing.value) return
  isDrawing.value = false
  strokes.value.push([...currentStroke.value])
  currentStroke.value = []
}

const recognize = () => {
  const matcher = new HanziLookup.Matcher('mmah')
  const matches = matcher.match(strokes.value, 8)
  candidates.value = matches.map(m => m.character)
}

const clear = () => {
  strokes.value = []
  candidates.value = []
  ctx.value.clearRect(0, 0, canvasRef.value.width, canvasRef.value.height)
}
</script>
```

---

## 📱 响应式设计要点

```css
/* tailwind.config.js 断点配置 */
module.exports = {
  theme: {
    screens: {
      'sm': '640px',   // 手机横屏
      'md': '768px',   // 平板
      'lg': '1024px',  // 小桌面
      'xl': '1280px',  // 大桌面
    }
  }
}
```

### 关键适配点

| 组件 | 移动端 | 桌面端 |
|------|--------|--------|
| 导航栏 | 底部Tab栏 | 顶部水平导航 |
| 搜索框 | 全宽 | 居中 max-w-xl |
| 汉字卡片 | 单列 | 双列 |
| 诗词列表 | 单列卡片 | 网格布局 |
| 手写板 | 全屏弹窗 | 侧边面板 |

---

## ⚡ 性能优化策略

### 1. 数据加载

```javascript
// 按需加载诗词数据
const loadPoetryChunk = async (type, page) => {
  const module = await import(`../data/poetry/${type}_${page}.json`)
  return module.default
}
```

### 2. 图片优化

- 字根图片使用 WebP 格式
- 实现懒加载
- 使用 CDN 加速

### 3. 缓存策略

```javascript
// Service Worker 缓存策略
const CACHE_NAME = 'hanzi-collection-v1'
const CACHE_URLS = [
  '/data/wubi86.json',
  '/data/tang300.json',
  '/data/song300.json',
  '/data/handwriting/mmah.json'
]
```

---

## 🌐 部署方案

### 推荐：Cloudflare Pages

**优势**：
- 免费额度充足
- 全球CDN，国内访问友好
- 自动 HTTPS
- Git 自动部署

**部署步骤**：
1. 代码推送至 GitHub
2. Cloudflare Dashboard 连接仓库
3. 配置构建命令：`npm run build`
4. 输出目录：`dist`
5. 绑定自定义域名（可选）

### 备选方案

| 平台 | 优势 | 劣势 |
|------|------|------|
| Vercel | 部署简单，预览方便 | 国内访问略慢 |
| Netlify | 功能丰富 | 国内访问一般 |
| GitHub Pages | 完全免费 | 国内访问较慢 |

---

## 📝 后续扩展方向

1. **成语词典**：集成成语释义、出处、用法
2. **歇后语**：民间智慧语录
3. **书法字帖**：生成可打印的练习字帖
4. **语音朗读**：诗词朗诵功能
5. **社区功能**：用户分享学习心得
6. **移动端 App**：基于 Capacitor 打包

---

## 📚 参考资源

### 数据源
- [86wubi](https://github.com/wongsingfo/86wubi) - 86五笔练习与数据
- [chinese-poetry](https://github.com/chinese-poetry/chinese-poetry) - 最全古诗词数据库
- [Make Me a Hanzi](https://github.com/skishore/makemeahanzi) - 汉字笔画数据

### 核心库
- [hanzi-writer](https://github.com/chanind/hanzi-writer) - 汉字笔顺动画
- [pinyin-pro](https://github.com/zh-lx/pinyin-pro) - 拼音处理
- [HanziLookupJS](https://github.com/gugray/HanziLookupJS) - 手写识别

### 设计参考
- [汉典](https://www.zdic.net/) - 汉字查询
- [古诗文网](https://www.gushiwen.cn/) - 诗词展示
- [西窗烛](https://www.xichuangzhu.com/) - 诗词App

---

*文档版本：v1.0*  
*最后更新：2025-12-02*
